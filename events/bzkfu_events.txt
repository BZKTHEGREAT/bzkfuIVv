add_namespace = bzkfu

country_event = {
	id = bzkfu.1
	title = bzkfu.1.a
	desc = bzkfu.1.b
	picture = GFX_report_event_JAP_warning
	fire_only_once = yes

	 trigger = {
OR = {
        tag = JAP
        tag = GER
        tag = ITA
        tag = FRA
        tag = ENG
        tag = SPR
        tag = USA
        tag = SOV
        tag = CAN
        tag = SAF
        tag = AST
        tag = RAJ
        tag = HUN
        tag = ROM
}
        date > 1942.1.1
    }

	option = {
		name = bzkfu.1.a
		add_ideas = JAP_bzkfu
	}
	mean_time_to_happen = { days = 2230 }
}